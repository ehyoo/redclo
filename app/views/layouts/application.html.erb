<!DOCTYPE html>
<html>
<head>
  <title>Redclo</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <a class="navbar-brand" href="/">RedClo</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">

      <ul class="nav navbar-nav navbar-right">


        <% if logged_in? %>
          <li><%= link_to "Log out", '/logout', method: 'delete' %></li>
        <% else %>
          <li><a href="/login">Log In</a></li>
          <li><a href="/signup">Sign Up</a></li>
        <% end %>


        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Subderrits <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <%Subderrit.all.each do |sub|%>
              <li><%= link_to sub.name, "/subderrits/#{sub.id}" %></li>
            <%end%>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>

<%= yield %>

</body>
</html>
